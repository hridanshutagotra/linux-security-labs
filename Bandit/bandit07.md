## Level 7
The password for the next level is stored in the file data.txt next to the word millionth

## Requirements
-  grep
- pipe (|) operator

## Solution
```bash
# we can use the pipe operator to give the output of the grep command to cat to display it

$ cat data.txt | grep millionth
